# ---------------------------------------------------------------------------
# Was NICHT auf GitHub darf.
#
# Grundregel: Auf GitHub liegt nur, was die Website zum Laufen braucht, also
# der Ordner website/ plus worker.js, wrangler.jsonc und package.json.
# Alles andere bleibt lokal.
# ---------------------------------------------------------------------------

# Internes Material: Originalfotos, Logos, Skizzen, Anleitungen, Verträge
Intern/

# Zugang zum Wartungsmodus: das PDF enthält den Freischalt-Schlüssel,
# die cmd-Dateien sind Werkzeuge für den eigenen Rechner
# (das zugehörige wartung.ps1 liegt in Intern/ und ist damit schon ausgeschlossen)
Wartungsmodus-Links.pdf
wartung-an.cmd
wartung-aus.cmd

# Zugangsdaten und lokale Wrangler-Dateien
.dev.vars*
!.dev.vars.example
.env*
!.env.example
.wrangler

# Werkzeuge und Editor
.claude/
node_modules/

# OneDrive / Windows
desktop.ini
Thumbs.db
